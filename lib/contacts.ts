/**
 * Единственный источник контактов Savage Movie.
 *
 * Заведён потому, что номер уже разъехался: в каталогах компания указана с
 * тремя разными телефонами, а в репозитории `tel:+79214021839` лежал в двух
 * файлах, ни один из которых не подключён к дереву — на живом сайте телефона
 * не было вообще. Здесь одна запись, которую используют и разметка, и вёрстка.
 *
 * Публичного офиса у студии нет, поэтому адреса тут нет и быть не должно:
 * география выражается через areaServed в JSON-LD, а не через PostalAddress.
 */

/** Как номер показывается человеку. */
export const PHONE_DISPLAY = '+7 921 402-18-39'

/** Как номер уходит в href и в schema.org: только цифры и плюс. */
export const PHONE_HREF = 'tel:+79214021839'

/** E.164 для JSON-LD. */
export const PHONE_E164 = '+79214021839'

export const EMAIL = 'hello@savagemovie.ru'
export const EMAIL_HREF = `mailto:${EMAIL}`
